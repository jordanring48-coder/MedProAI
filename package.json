{
  "name": "medchron",
  "version": "0.1.0",
  "private": true,
  "description": "MedTrack AI — Smart medication management app",
  "scripts": {
    "dev": "concurrently -n frontend,backend -c blue,green \"bun run --cwd frontend dev\" \"bun run --cwd backend dev\"",
    "build": "bun run --cwd frontend build && bun run --cwd backend build",
    "start": "bun run --cwd backend start",
    "publish": "bash publish.sh"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
